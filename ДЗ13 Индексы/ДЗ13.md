### Домашнее задание 13 ###
1. Создадим таблицу: ![](Create_table.jpg), 
1. Создаем индекс по полю about: create  index player_name on players(about);


1. Запрос explain(costs, verbose)  
select about from players where about  = 'Player.Player.Number is 8319'; возвращает  
Index Only Scan using player_name on public.players  (cost=0.29..4.32 rows=2 width=31)
  Output: about
  Index Cond: (players.about = 'Player.Player.Number is 8319'::text)
1.Реализация полнотекстового индекса: 

alter table players add column about_lexeme tsvector;

update players
set about_lexeme = to_tsvector(about);

CREATE INDEX search_index_about ON players USING GIN (about_lexeme);

explain select age, about from players where about_lexeme @@ to_tsquery('1285| 1280' );![](gin.jpg)

4. Индекс по двум полям: 

![](two_fields.jpg)

5. Индекс на часть таблицы:

![](index_part.jpg)

6. Вывод : использование  индексов на малых таблицах  (менее 1000 записей) не дает выигрыша. 




